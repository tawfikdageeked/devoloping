#include <iostream>
#include <cmath>

int main(){

    double x = 3.14;
    double y = 4;

    double z = std::min(x, y); // tells you the which is smaller
    
    double a = std::max(x, y); // tells you which is bigger

    double l = pow(3, 2); // 3 to the power of 2

    double j = sqrt(9); // square root of 9 

    double h = abs(-4); // the abslute value of -4
    
    double b = round(x); // round down to 3


    double n = ceil(x); // round up to 4

    // for more go to www.cplusplus.com/reference/cmath




}